#include<cstdio>
#include<iostream>
#inclue<algorithm>

using namespace std;

int doc()
{
    scanf("%d%d",&n,&m);
    for(i,1,m){
        int u,v;
        scanf("%d%d",&u,&v);
        ke[v].push_back(u);
    }
    for(i,1,n)
        if(dd[i])
}
int main()
{
    int t;
    scanf("%d",&t);
    for(i,1,t){
        doc();
        xuly();
    }

}
